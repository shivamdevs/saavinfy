import usePlayerBuffer from "./use-buffer";
import usePlayerNavigation from "./use-navigation";
import usePlayerOptions from "./use-options";
import usePlayerState from "./use-state";
import usePlayerTiming from "./use-timing";
import usePlayerVolume from "./use-volume";

const Player = {
    usePlayerBuffer,
    usePlayerNavigation,
    usePlayerOptions,
    usePlayerState,
    usePlayerTiming,
    usePlayerVolume,
};

export default Player;

export {
    usePlayerBuffer,
    usePlayerNavigation,
    usePlayerOptions,
    usePlayerState,
    usePlayerTiming,
    usePlayerVolume,
};
