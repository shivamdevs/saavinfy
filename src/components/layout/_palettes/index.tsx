// "use client";

// import React from "react";
// import { usePlayerOptions } from "@/contexts/player/hooks";
// import QueuePalette from "./queue";
// import InfoPalette from "./info";

// function Palettes() {
//     const { ...options } = usePlayerOptions();

//     if (!options.panel) return null;

//     return (
//         <aside className="w-96 layout-card relative layout-col gap-0">
//             {options.panel === "queue" && <QueuePalette />}
//             {options.panel === "info" && <InfoPalette />}
//         </aside>
//     );
// }

// export default Palettes;
